
# This is `bat`s configuration file. Each line either contains a comment or
# a command-line option that you want to pass to `bat` by default. You can
# run `bat --help` to get a list of all possible configuration options.

# Specify desired highlighting theme (e.g. "TwoDark"). Run `bat --list-themes`
# for a list of all available themes
--theme="Dracula"

# Enable this to use italic text on the terminal. This is not supported on all
# terminal emulators (like tmux, by default):
--italic-text=always

# Uncomment the following line to disable automatic paging:
#--paging=never

# Uncomment the following line if you are using less version >= 551 and want to
# enable mouse scrolling support in `bat` when running inside tmux. This might
# disable text selection, unless you press shift.
--pager="less --RAW-CONTROL-CHARS --quit-if-one-screen --mouse"

# Set the tab width to T spaces. Use a width of 0 to pass tabs through directly
--tabs 4

# Syntax mappings: map a certain filename pattern to a language.
#   Example 1: use the C++ syntax for Arduino .ino files
#   Example 2: Use ".gitignore"-style highlighting for ".ignore" files
#--map-syntax "*.ino:C++"
#--map-syntax ".ignore:Git Ignore"

# Format: Bourne Again Shell (bash)
--map-syntax "bash_completion:Bourne Again Shell (bash)"
--map-syntax "dashrc:Bourne Again Shell (bash)"
--map-syntax "bashrc:Bourne Again Shell (bash)"
--map-syntax "bash_profile:Bourne Again Shell (bash)"
--map-syntax "bash_history:Bourne Again Shell (bash)"
--map-syntax "zprofile:Bourne Again Shell (bash)"
--map-syntax "zlogin:Bourne Again Shell (bash)"
--map-syntax "zlogout:Bourne Again Shell (bash)"
--map-syntax "zshenv:Bourne Again Shell (bash)"
--map-syntax "zshrc:Bourne Again Shell (bash)"
--map-syntax "xinitrc:Bourne Again Shell (bash)"

# Format: CSS
--map-syntax "*.rasi:CSS"

# Format: DotENV
--map-syntax "/etc/environment:DotENV"
--map-syntax "/etc/environment.d/*:DotENV"
